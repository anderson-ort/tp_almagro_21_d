export default class ProductModelSupabase {

    constructor(id
        , nombre
        , precio
        , descripcion
        , embedding) {
        this.id = id
        this.nombre = nombre
        this.precio = precio
        this.descripcion = descripcion
        this.embedding = embedding
    }

}